#import "../lib.typ": *

#show: doc => bwp-book(doc)

= Introduction

Most builders do not fail because they cannot execute. They fail because they choose weak ideas.

#bwp-callout[
The strongest early signal is not “people care about this topic.” It is “people are already paying, losing money, or burning serious time because this problem is unresolved.”
]

= Framework in Action

#bwp-opportunity-card(
  [SauceStack],
  [first-time hot sauce entrepreneurs],
  [state-by-state cottage food confusion, labeling uncertainty, and co-packer questions],
  [compliance navigator plus label requirement checker],
  [clear buyer, urgent trigger, obvious willingness to pay],
)

#pagebreak()
#bwp-scorecard-page()

#pagebreak()
#bwp-worksheet-page()
